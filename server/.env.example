# Server config
PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# MongoDB config
MONGODB_URI=mongodb://127.0.0.1:27017/qa_eval
# Optional separate db name (overrides db in URI)
# MONGODB_DB=qa_eval

# Seed default user
SEED_USERNAME=test
SEED_PASSWORD=password
SEED_NAME=Test User
