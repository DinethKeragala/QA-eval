{
  "name": "qa-eval",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "server",
    "client",
    "tests"
  ],
  "scripts": {
  "start:server": "npm --workspace server run start",
  "start:client": "npm --workspace client run dev",
  "start": "concurrently -k -n server,client -c blue,green \"npm run start:server\" \"npm run start:client\"",
  "build": "npm -w client run build",
  "test": "cross-env USE_IN_MEMORY_DB=true CLIENT_URL=http://localhost:5173 start-server-and-test start http://localhost:5173 \"npm -w tests test\"",
  "test:selenium": "cross-env USE_IN_MEMORY_DB=true CLIENT_URL=http://localhost:5173 start-server-and-test start http://localhost:5173 \"npm run selenium:test\"",
  "selenium:test": "mocha tests/**/*.spec.js --timeout 10000",
  "api:test:local": "newman run ./postman/express-task-api-tests.postman_collection.json",
  "api:test": "start-server-and-test start:server http://localhost:4000/health \"npm run api:test:local\""
}
  ,
  "devDependencies": {
    "chai": "^6.2.0",
    "concurrently": "^9.0.0",
    "cross-env": "^7.0.3",
    "mocha": "^11.7.2",
    "newman": "^6.2.0",
    "selenium-webdriver": "^4.35.0",
    "start-server-and-test": "^2.0.2"
  },
  "dependencies": {
    "vite": "7.1.7",
    "vitest": "3.2.4"
  }
}
